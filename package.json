{
  "name": "calculate-financial",
  "version": "1.0.0",
  "description": "Library to calculate financial data of company",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "compile": "babel --presets es2015,stage-0 index.js -d dist/",
    "prepublish": "npm run compile",
    "coverage": "jest --coverage"
  },
  "dependencies": {
    "jest": "^20.0.4"
  },
  "devDependencies": {},
  "author": "F(x)",
  "license": "ISC"
}
